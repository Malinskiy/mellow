---
description: Build Mellow debug APK
agent: build
---

Build the debug APK and report any compilation errors. If errors occur, show the relevant code and suggest fixes.

```bash
./gradlew assembleDebug
```
